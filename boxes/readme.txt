Pack planner test project.

How to run:
1. Install Maven3.
2. Open command line or shell, run command "mvn clean package".
3. Go to ./target directory and run the app "java -jar boxes-fat-1.0.jar" or stdin dataset file "java -jar boxes-fat-1.0.jar < input".
4. Check the results.
